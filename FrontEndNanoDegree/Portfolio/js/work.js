/*
 * Open the featured work description when the info icon is clicked.
 * Close the drawer when the body is clicked.
 */

function toggleDescription(element) {
  element.classList.toggle('active');
  e.stopPropagation();
}
